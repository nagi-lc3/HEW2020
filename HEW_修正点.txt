修正点
・すべてのパスを相対パスに修正


ranking.php
    ・var_dump($link);を消し忘れていたので削除
    ・商品取り出しのsqlの部分が間違っていたので修正
    ・画像表示の部分が間違っていたので修正

products.php
    ・商品がない場合にページネーションが表示されないように修正
	    if(!empty($products)) { // 追加
	    // ページネーション
	    } // 追加

・model_view2.htmlの内容をaquarium_createへ移しかえ
・model_view2_forCSV.htmlの内容をaquarium_1へ移しかえ
・csv_receive_and_save.phpを追加
・csvsフォルダを追加



・ログインを強制するように遷移できる自分の担当分(購入関連以外)のページに
    session_start();

    // ログインしていない場合
    if ($_SESSION['user_name'] == false) {
        header('Location: ./login.php');
    }
    を追加

--------------------------------------------------
・HEW_test.sqlのほうにデータが入っていますのでこれでDB作成してください
・ID:test1@test.com　password：password1!でログインできると思います
　出来なかったら新規登録してください(先にquestionsテーブルにデータが入っていないとエラーが出ます)
・adminは手はつけていますがまだ途中です
・一応バックはこれで提出出来ると思いますが何かエラーが出たら教えてください